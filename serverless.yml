service: hello

useDotenv: true

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev

functions:
  hello:
    handler: src/hello.handler
    events:
      - httpApi: GET /hello

plugins:
  - serverless-esbuild
  - serverless-offline
